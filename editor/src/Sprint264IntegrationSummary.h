#pragma once

// Sprint 264 integration summary:
// - Aligned raw candidate score path with downstream impact coverage by persisting selected tasks into generation artifacts.
// - Removed scorer/gate parity mismatch observed in sprint 263.
// - Structural projector path now yields consistent pass signals across score and gate on the hard sample.
