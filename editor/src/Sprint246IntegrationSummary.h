#pragma once

// Sprint 246 integration summary:
// - Added intrinsic multishot generator decomposition in pipeline.
// - Multishot runs profile-targeted generation passes and merges resulting tasks.
// - Enforced impact coverage can now pass with autofill disabled when multishot is enabled.
